#include<stdio.h>
#include<math.h>
int main(){
    int n, i; 
    long long tong = 0;
    scanf("%d",&n);
    for ( i=1; i <= n; i++)
    {
        tong += i * i;    
    }
    printf("%lld", tong);
    return 0;
}